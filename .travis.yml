language: python
python:
 - "3.5"
 - "3.6"

install:
 - pip install coveralls
 - pip install coverage
 - pip install .
 
script:
 - pytest
 - coverage run --source=arrayqueues
 
after_success:
 - coveralls
